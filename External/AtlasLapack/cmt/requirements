package AtlasLapack
author  Sebastien Binet <binet@cern.ch>

## For Athena policies: it has to be the first use statement
use AtlasPolicy         AtlasPolicy-*
use AtlasBlas		AtlasBlas-*		External

use lapack		*		LCG_Interfaces

# use PyUtils		PyUtils-*		Tools -no_auto_imports

# macro_append AtlasLapack_linkopts " -llapack"
# #include_path none
# #apply_pattern include_path extras=$(CMTINSTALLAREA)/$(CMTCONFIG)/include
# include_dirs $(CMTINSTALLAREA)/$(CMTCONFIG)/include

# private

# use AtlasFortranPolicy  AtlasFortranPolicy-*
# apply_pattern make_pkgbuild \
#  name=lapack \
#  file=pkgbuild_lapack.py

# # will pick the lapack library from system...
# apply_pattern disable_package_on platform=Darwin

# end_private

private
use ExternalPolicy ExternalPolicy-* External
macro AtlasLapack_export_paths "$(lapack_home)"
macro AtlasLapack_lcgcmt_lib "$(lapack_home)/lib"
apply_pattern declare_installed_libraries dir=$(AtlasLapack_lcgcmt_lib)
end_private
macro AtlasLapack_native_version "$(lapack_native_version)"
