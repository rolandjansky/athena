# Print all cint tag names.

define dump_cinttags
  shared libCint
  set logging file $arg0
  set logging redirect on
  set logging on
  set pagination off
  set $i=0
  while $i<G__struct.alltag
    print G__struct.name[$i]
    set $i=$i+1
  end
  set pagination on
  set logging redirect off
  set logging off
end
