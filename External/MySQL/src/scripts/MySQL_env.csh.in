if ( "$?MySQLTOP" == 0 ) then

        setenv MySQLTOP ${ATLAS_EXTERNAL}/MySQL
	
endif


      setenv My_SQL ${MySQLTOP}/XXXMySQL_VERSIONXXX 
      
      if ( $?LD_LIBRARY_PATH ) then
      
          setenv  LD_LIBRARY_PATH ${My_SQL}/lib:$LD_LIBRARY_PATH
	  
      else 
       
         setenv  LD_LIBRARY_PATH ${My_SQL}/lib
	 
      endif
      
