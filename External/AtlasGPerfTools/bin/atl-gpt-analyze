#!/bin/sh

# run like so:
#   atl-gpt-analyze my.profile
function atl_gpt_analyze()
{
    profile=$1; shift;

    pprof --callgrind `which python` $profile >| ${profile}.callgrind
    sc=$?
    return $sc
}

atl_gpt_analyze "$@"
