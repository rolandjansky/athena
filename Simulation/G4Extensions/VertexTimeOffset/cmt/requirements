package VertexTimeOffset

version VertexTimeOffset-00-00-00

branches src cmt

use AtlasPolicy         AtlasPolicy-*

private
use AtlasReflex         AtlasReflex-*           External
use FadsKinematics      FadsKinematics-*     	Simulation/G4Sim/FADS
use GaudiInterface 	GaudiInterface-* 	External
use StoreGate		StoreGate-*		Control
use SGTools		SGTools-*		Control
use Geant4		Geant4-*		External
use AtlasCLHEP  AtlasCLHEP-*    External
end_private

include_dirs "$(VertexTimeOffset_root)" "$(VertexTimeOffset_root)/dict"

library VertexTimeOffset *.cxx 
apply_pattern component_library

private
apply_pattern lcgdict dict=VertexTimeOffset \
                headerfiles="../VertexTimeOffset/VertexTimeOffset.h" \
                selectionfile=selection.xml

macro_prepend VertexTimeOffsetDict_shlibflags " -lVertexTimeOffset " 
end_private

