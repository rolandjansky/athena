package CaloHiveEx

use AtlasPolicy   AtlasPolicy-*

private
end_private

# library CaloHiveEx *.cxx -s=components *.cxx
# apply_pattern component_library

apply_pattern declare_joboptions files="*.py"