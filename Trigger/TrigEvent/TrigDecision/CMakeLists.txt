################################################################################
# Package: TrigDecision
################################################################################

# Declare the package name:
atlas_subdir( TrigDecision )

# Install files from the package:
atlas_install_headers( TrigDecision )
atlas_install_python_modules( python/__init__.py python/TrigDecisionConfig.py )

