################################################################################
# Package: AtlasSystemSettings
################################################################################

# Declare the package name:
atlas_subdir( AtlasSystemSettings )

# this line failed automatic conversion in cmt2cmake :
# tag gcc gcc3

athena_env( SET atlas_config_extras \"\" )

