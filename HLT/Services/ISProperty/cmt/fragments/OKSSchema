#============ genOKSSchema ===================================== 

$(outdir)/$(library).schema.xml : ${FULLNAME} ${CONSTITUENT}Dirs
	@$(genOKSCmd) \
	  --package-name=$(library) \
	  --output-dir=$(outdir) \
	  --input-libraries=${FULLNAME} ;

#======================================================

