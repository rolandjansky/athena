#-- start of cleanup_header --------------

clean :: ${CONSTITUENT}clean ;
#	@cd .

ifndef PEDANTIC
.DEFAULT::
	$(echo) "(${CONSTITUENT}.make) $@: No rule for such target" >&2
else
.DEFAULT::
	$(error PEDANTIC: $@: No rule for such target)
endif

${CONSTITUENT}clean ::
#-- end of cleanup_header ---------------
