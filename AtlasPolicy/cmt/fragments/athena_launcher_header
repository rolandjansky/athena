
${CONSTITUENT} ::
	cd ../run; \
	${athena_pre}; \
	if [ "${athena_rem}" != "" ]; then \
	    if test -f ${athena_rem}; then \
		/bin/rm ${athena_rem}; \
            fi; \
	fi; \
	${athena_exe} ${athena_opt} ${athena_out}; \
	${athena_post}; \
	echo "${CONSTITUENT} completed"


